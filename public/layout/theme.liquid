<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="#111111">
    <link rel="canonical" href="{{ canonical_url }}">
    <link rel="preconnect" href="https://cdn.shopify.com" crossorigin>

    {%- if settings.favicon != blank -%}
      <link rel="icon" type="image/png" href="{{ settings.favicon | image_url: width: 32, height: 32 }}">
    {%- endif -%}

    <title>
      {{ page_title }}
      {%- if current_tags %} &ndash; tagged "{{ current_tags | join: ', ' }}"{% endif -%}
      {%- if current_page != 1 %} &ndash; Page {{ current_page }}{% endif -%}
      {%- unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless -%}
    </title>

    {% if page_description %}
      <meta name="description" content="{{ page_description | escape }}">
    {% endif %}

    {% render 'meta-tags' %}

    {{ content_for_header }}

    <!-- Global Styles: Tactical Noir -->
    <style>
      :root {
        --color-void: #111111;
        --color-charcoal: #1a1a1a;
        --color-gold: #ffd700;
        --color-white: #ffffff;
        --color-text-muted: #999999;
        --font-heading: 'Impact', 'Arial Black', sans-serif;
        --font-body: 'Courier New', Courier, monospace;
      }
      body {
        background-color: var(--color-void);
        color: var(--color-white);
        font-family: sans-serif;
        margin: 0;
        line-height: 1.6;
      }
      a {
        color: var(--color-gold);
        text-decoration: none;
        transition: color 0.2s ease;
      }
      a:hover {
        color: #fff;
      }

      /* TACTICAL HEADER */
      .site-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0 2rem;
        background: rgba(17, 17, 17, 0.95);
        border-bottom: 1px solid #333;
        position: sticky;
        top: 0;
        z-index: 1000;
        height: 80px;
        backdrop-filter: blur(10px);
      }
      
      .logo img {
        height: 50px;
        width: auto;
        display: block;
      }

      .site-nav ul {
        display: flex;
        gap: 3rem;
        list-style: none;
        margin: 0;
        padding: 0;
      }
      .site-nav a {
        color: #fff;
        font-weight: 700;
        text-transform: uppercase;
        font-size: 0.9rem;
        letter-spacing: 2px;
        display: block;
        padding: 1.5rem 0;
        position: relative;
      }
      .site-nav a::after {
        content: '';
        position: absolute;
        bottom: 1rem;
        left: 0;
        width: 0;
        height: 2px;
        background: var(--color-gold);
        transition: width 0.3s ease;
      }
      .site-nav a:hover::after {
        width: 100%;
      }
      .site-nav a.active {
        color: var(--color-gold);
      }

      /* Primary CTA Glow */
      .nav-play {
        color: var(--color-gold) !important;
        text-shadow: 0 0 10px rgba(255, 215, 0, 0.3);
      }

      /* Dropdown Menu */
      .has-dropdown {
        position: relative;
      }
      .dropdown-menu {
        display: none;
        position: absolute;
        top: 100%;
        left: 50%;
        transform: translateX(-50%);
        background: var(--color-charcoal);
        border: 1px solid #333;
        border-top: 2px solid var(--color-gold);
        min-width: 280px;
        padding: 1.5rem 0;
        z-index: 200;
        box-shadow: 0 10px 30px rgba(0,0,0,0.5);
      }
      .has-dropdown:hover .dropdown-menu {
        display: block;
      }
      .dropdown-menu li {
        display: block;
        padding: 0 2rem;
      }
      .dropdown-menu a {
        text-transform: none;
        color: #ccc;
        padding: 0.5rem 0;
        font-size: 0.9rem;
        letter-spacing: 0.5px;
        border-bottom: none;
      }
      .dropdown-menu a::after { display: none; }
      .dropdown-menu a:hover {
        color: var(--color-gold);
        padding-left: 5px;
      }

      /* Sub-headers in Dropdown */
      .dropdown-header {
        color: var(--color-gold);
        font-size: 0.75rem;
        text-transform: uppercase;
        padding: 0.5rem 2rem 0.2rem;
        margin-top: 1rem;
        font-weight: bold;
        opacity: 0.6;
        letter-spacing: 1px;
      }
      .dropdown-header:first-child { margin-top: 0; }

      /* Header Icons */
      .header-icons {
        display: flex;
        gap: 1.5rem;
        align-items: center;
      }
      .icon-link {
        color: #fff;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 0.8rem;
        font-weight: bold;
        text-transform: uppercase;
        letter-spacing: 1px;
      }
      .icon-link svg {
        width: 20px;
        height: 20px;
        fill: currentColor;
      }

      /* Footer */
      .site-footer {
        padding: 4rem 2rem;
        text-align: center;
        background: #000;
        margin-top: 6rem;
        border-top: 1px solid #333;
        color: #666;
        font-size: 0.9rem;
      }
      
      /* Mobile Menu Toggle (Hidden on Desktop) */
      .mobile-menu-toggle { display: none; }

      @media (max-width: 768px) {
        .site-nav { display: none; } /* Hide standard nav on mobile for now */
        .mobile-menu-toggle { display: block; color: #fff; font-size: 1.5rem; }
      }
    </style>
  </head>

  <body class="gradient">
    <a class="skip-to-content-link button visually-hidden" href="#MainContent">
      {{ 'accessibility.skip_to_text' | t }}
    </a>

    <!-- Header -->
    <header class="site-header">
      <div class="logo">
        <a href="/" class="header-logo-link">
          <img src="{{ 'wolfpack-logo.png' | asset_url }}" alt="{{ shop.name }}">
        </a>
      </div>

      <nav class="site-nav">
        <ul>
          <li><a href="/pages/game" class="nav-play">PLAY</a></li>
          
          <!-- Shop Dropdown -->
          <li class="has-dropdown">
            <a href="/collections/all">SHOP</a>
            <ul class="dropdown-menu">
              <li class="dropdown-header">Fuel</li>
              <li><a href="/collections/all-coffee-tea-prods">Coffee & Tea</a></li>
              <li><a href="/collections/sample-packs">Sample Packs</a></li>
              
              <li class="dropdown-header">Gear</li>
              <li><a href="/collections/apparel">Apparel</a></li>
              <li><a href="/collections/accessories">Accessories</a></li>
              
              <li class="dropdown-header">Protocol</li>
              <li><a href="/pages/wholesale">Wholesale / Bulk</a></li>
            </ul>
          </li>

          <li><a href="/pages/watch">WATCH</a></li>
          <li><a href="/pages/about">ABOUT</a></li>
        </ul>
      </nav>

      <div class="header-icons">
        <a href="/search" class="icon-link">
          <svg viewBox="0 0 24 24"><path d="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"/></svg>
        </a>
        <a href="/cart" class="icon-link">
          <svg viewBox="0 0 24 24"><path d="M7 18c-1.1 0-1.99.9-1.99 2S5.9 22 7 22s2-.9 2-2-.9-2-2-2zM1 2v2h2l3.6 7.59-1.35 2.45c-.16.28-.25.61-.25.96 0 1.1.9 2 2 2h12v-2H7.42c-.14 0-.25-.11-.25-.25l.03-.12.9-1.63h7.45c.75 0 1.41-.41 1.75-1.03l3.58-6.49c.08-.14.12-.31.12-.48 0-.55-.45-1-1-1H5.21l-.94-2H1zm16 16c-1.1 0-1.99.9-1.99 2s.89 2 1.99 2 2-.9 2-2-.9-2-2-2z"/></svg>
          <span>CART</span>
        </a>
      </div>
    </header>

    <main id="MainContent" class="content-for-layout focus-none" role="main" tabindex="-1">
      {{ content_for_layout }}
    </main>

    <footer class="site-footer">
      <p>
        &copy; {{ 'now' | date: '%Y' }} {{ shop.name }}.<br>
        <span style="color: var(--color-gold); text-transform: uppercase; letter-spacing: 1px; font-size: 0.8rem; margin-top: 0.5rem; display: inline-block;">Fuel Your Hustle. Build Your Pack.</span>
      </p>
    </footer>

    <ul hidden>
      <li id="a11y-refresh-page-message">{{ 'accessibility.refresh_page' | t }}</li>
      <li id="a11y-new-window-message">{{ 'accessibility.link_messages.new_window' | t }}</li>
    </ul>

    <script>
      window.shopUrl = '{{ shop.url }}';
    </script>
  </body>
</html>
