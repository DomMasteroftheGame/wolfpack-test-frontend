<style>
  /* HERO SECTION: The Traffic Cop */
  .hero-section {
    text-align: center;
    padding: 6rem 1rem 4rem;
    background: #111;
    min-height: 30vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    border-bottom: 1px solid #333;
  }
  .hero-title {
    font-family: var(--font-heading);
    font-size: 4rem;
    color: #fff;
    margin-bottom: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    line-height: 1;
  }
  .hero-subtitle {
    font-family: var(--font-body);
    font-size: 1.1rem;
    color: var(--color-gold);
    max-width: 700px;
    margin: 1rem auto 0;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  /* TACTICAL PANELS */
  .command-center {
    display: grid;
    grid-template-columns: 1.5fr 1fr 1fr; /* Asymmetric Grid */
    gap: 0;
    max-width: 100%;
    margin: 0;
    border-bottom: 1px solid #333;
  }

  .tactical-panel {
    position: relative;
    height: 600px;
    border-right: 1px solid #333;
    overflow: hidden;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 3rem;
    background-size: cover;
    background-position: center;
  }
  .tactical-panel:last-child { border-right: none; }
  
  /* Panel Overlays */
  .tactical-panel::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(0,0,0,0.9) 0%, rgba(0,0,0,0.2) 50%, rgba(0,0,0,0.4) 100%);
    z-index: 1;
    transition: background 0.3s ease;
  }
  .tactical-panel:hover::before {
    background: linear-gradient(to top, rgba(0,0,0,0.95) 0%, rgba(0,0,0,0.1) 50%, rgba(0,0,0,0.2) 100%);
  }

  /* Panel Content */
  .panel-content {
    position: relative;
    z-index: 2;
    transform: translateY(0);
    transition: transform 0.3s ease;
  }
  .tactical-panel:hover .panel-content {
    transform: translateY(-10px);
  }

  .panel-label {
    font-family: var(--font-body);
    color: var(--color-gold);
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 0.5rem;
    display: block;
  }
  .panel-title {
    font-family: var(--font-heading);
    font-size: 3rem;
    color: #fff;
    margin: 0 0 1rem;
    line-height: 0.9;
    text-transform: uppercase;
  }
  .panel-hook {
    color: #ccc;
    font-size: 1rem;
    max-width: 90%;
    margin-bottom: 2rem;
    line-height: 1.4;
  }

  /* Specific Panel Styles */
  .panel-game {
    background-image: url('{{ "hero-bg.jpg" | asset_url }}'); /* Fallback */
    /* Add a digital grid overlay via CSS */
    background-color: #0a0a0a;
  }
  .panel-game .panel-title { font-size: 4rem; } /* Larger for dominant panel */

  .panel-shop {
    background-image: url('{{ "coffee-bag-hero.jpg" | asset_url }}');
    background-color: #111;
  }

  .panel-mission {
    background-image: url('{{ "about-hero.jpg" | asset_url }}');
    background-color: #151515;
  }

  /* Buttons */
  .btn-tactical {
    display: inline-block;
    padding: 1rem 2rem;
    border: 1px solid var(--color-gold);
    color: var(--color-gold);
    font-family: var(--font-heading);
    text-transform: uppercase;
    letter-spacing: 1px;
    font-size: 1.1rem;
    background: rgba(0,0,0,0.5);
    backdrop-filter: blur(5px);
    transition: all 0.2s ease;
  }
  .tactical-panel:hover .btn-tactical {
    background: var(--color-gold);
    color: #000;
  }

  .link-sub {
    display: block;
    margin-top: 1rem;
    color: #666;
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 1px;
    text-decoration: underline;
  }
  .link-sub:hover { color: #fff; }

  @media (max-width: 1024px) {
    .command-center { grid-template-columns: 1fr; }
    .tactical-panel { height: 400px; border-right: none; border-bottom: 1px solid #333; }
    .hero-title { font-size: 2.5rem; }
  }
</style>

<div class="hero-section">
  <h1 class="hero-title">Fuel Your Hustle.<br>Build Your Pack.</h1>
  <p class="hero-subtitle">Play the game to learn the strategy. Drink the coffee to fuel the execution.</p>
</div>

<div class="command-center">
  <!-- BLOCK 1: THE GAME (Dominant) -->
  <div class="tactical-panel panel-game" onclick="window.location.href='/pages/game'">
    <!-- Digital Grid Overlay -->
    <div style="position: absolute; inset: 0; background-image: linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.03) 1px, transparent 1px); background-size: 20px 20px; pointer-events: none;"></div>
    
    <div class="panel-content">
      <span class="panel-label">Operation Wolfpack</span>
      <h2 class="panel-title">Play The<br>Infinite Game</h2>
      <p class="panel-hook">Test your entrepreneurial skills. Win codes for free coffee. Join the leaderboard.</p>
      <span class="btn-tactical">Enter The Arena</span>
    </div>
  </div>

  <!-- BLOCK 2: THE SHOP -->
  <div class="tactical-panel panel-shop" onclick="window.location.href='/collections/all'">
    <div class="panel-content">
      <span class="panel-label">Premium Roast</span>
      <h2 class="panel-title">Fuel The<br>Pack</h2>
      <p class="panel-hook">High-altitude beans for high-performance humans.</p>
      <span class="btn-tactical">Shop Collection</span>
      <a href="/pages/wholesale" class="link-sub" onclick="event.stopPropagation()">Wholesale / Bulk Procurement</a>
    </div>
  </div>

  <!-- BLOCK 3: THE MISSION -->
  <div class="tactical-panel panel-mission" onclick="window.location.href='/pages/about'">
    <div class="panel-content">
      <span class="panel-label">The Architect</span>
      <h2 class="panel-title">Meet The<br>Alpha</h2>
      <p class="panel-hook">Read the story behind the movement.</p>
      <span class="btn-tactical">Briefing</span>
    </div>
  </div>
</div>
